#!/usr/bin/env node

'use strict';

var nano = require('nano')('http://localhost:5984');

nano.db.destroy('chapters', function (err, body) {
  if (!err) console.log('Database \'chapters\' deleted.');
  else console.log(err);
});
