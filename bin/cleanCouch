#!/usr/bin/env node

'use strict';

var nano = require('nano')('http://localhost:5984');

nano.db.destroy('stories', function (err, body) {
  if (!err) console.log('Database \'stories\' deleted.');
  else console.log(err);
});
